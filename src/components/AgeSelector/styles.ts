import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 20,
    marginBottom: 20,
  },

  slider: {
    width: '85%',
    color: '#549bef',
  },

  input: {
    fontSize: 23,
    color: '#1e5ba3',
    fontWeight: 'bold',
  },
});
