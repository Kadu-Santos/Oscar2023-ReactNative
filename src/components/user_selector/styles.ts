import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

  container: {
    padding: 16,
    marginTop: 200,
    alignItems: 'center',
  },

  selectedOptionText: {
    fontSize: 18,
    marginBottom: 10,
  },
  
  });
  