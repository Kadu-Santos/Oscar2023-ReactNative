import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 100,
  },

  slider: {
    width: '80%',
  
  },

  selectedValueText: {
    marginTop: 20,
    fontSize: 18,
    color: 'black',
  },

  input: {
    marginTop: 0,
    fontSize: 23,
    textAlign: 'center',
    width: '80%',
    height: 40,
  },
});
