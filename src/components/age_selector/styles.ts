import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',

  },

  slider: {
    width: '80%',
    trackHeight: 50,
  },

  input: {
    fontSize: 23,
    textAlign: 'center',
    width: '80%',
    height: 40,
    color: 'black',
  },
});
