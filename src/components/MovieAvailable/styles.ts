import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

  containerAvailable: {
    flexDirection: 'row',
    marginTop: 20,
    marginBottom: 20,
    justifyContent: 'center',
  },


  iconAvailable: {
    width: 40,
    height: 40,
    margin: 5,
    borderRadius: 7,
  },

});
